# Ferie Manager

App React + Vite per la gestione del piano ferie.

## Variabili ambiente

Imposta in Netlify:

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

## Avvio locale

```bash
npm install
npm run dev
```

## Deploy Netlify

- Build command: `npm run build`
- Publish directory: `dist`
